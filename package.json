{
  "name": "my-ci-demo",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --coverage",
    "lint": "eslint src tests",
    "build": "echo 'Build successful' && mkdir -p dist && cp -r src dist/"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.4"
  }
}
